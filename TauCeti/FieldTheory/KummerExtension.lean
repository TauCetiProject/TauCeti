/-
Copyright (c) 2026 The Tau Ceti contributors. All rights reserved.
Released under Apache 2.0 license as described in the file LICENSE.
Authors: The Tau Ceti contributors
-/
module

public import Mathlib.FieldTheory.IntermediateField.Adjoin.Basic
public import Mathlib.FieldTheory.KummerPolynomial
import Mathlib.FieldTheory.SeparableDegree
public import TauCeti.RingTheory.Valuation.Discrete.Order

/-!
# Square roots and the binomial `X ^ n - C a`

Mathlib's Kummer theory of `X ^ n - C a` (in `Mathlib/FieldTheory/KummerExtension.lean`) runs
through a primitive `n`-th root of unity, which is unavailable for `n = 2` in characteristic `2`.
This file records the elementary facts that need no root of unity: membership in the root set of
`X ^ n - C a` is the equation `x ^ n = a`, and for `n = 2` a single square root `δ` of `a` already
splits the binomial and generates its splitting field, because the only other root is `-δ`.

## Main results

* `Polynomial.mem_rootSet_X_pow_sub_C`: a point of an extension is a root of `X ^ n - C a` exactly
  when its `n`-th power is `a`.
* `IntermediateField.adjoin_rootSet_X_pow_two_sub_C`: a square root of `a` generates the root-set
  adjunction of `X ^ 2 - C a`.
* `Polynomial.splits_map_X_pow_two_sub_C`: a square root of `a` in an extension splits
  `X ^ 2 - C a` there.
* `Valuation.X_pow_sub_C_irreducible_of_gcd_ord_eq_one`: `X ^ n - C a` is irreducible when `n`
  is coprime to the order of `a` at a discrete valuation.
* `Valuation.finrank_eq_of_pow_eq_of_gcd_ord_eq_one`: a radical extension generated by a root
  of `X ^ n - C a` has degree `n` under the same coprimality condition.
* `TauCeti.Algebra.finiteDimensional_of_pow_eq` and
  `TauCeti.Algebra.isSeparable_of_pow_eq`: finiteness and separability of a radical extension
  generated by a root of `X ^ n - C a`.

## References

* H. Stichtenoth, *Algebraic Function Fields and Codes*, 2nd ed., GTM 254, Springer, 2009,
  Proposition 3.7.3.
-/

public section

open Polynomial

open scoped IntermediateField WithZero

namespace TauCeti

universe u v

section RootSet

variable {F : Type u} [CommRing F] {E : Type v} [Field E] [Algebra F E] {a : F}

/-- A point of an extension is a root of `X ^ n - C a` exactly when its `n`-th power is `a`. -/
@[simp]
theorem _root_.Polynomial.mem_rootSet_X_pow_sub_C {n : ℕ} (hn : n ≠ 0) {x : E} :
    x ∈ ((X : F[X]) ^ n - C a).rootSet E ↔ x ^ n = algebraMap F E a := by
  rw [(monic_X_pow_sub_C a hn).mem_rootSet, map_sub, aeval_X_pow, aeval_C,
    sub_eq_zero]

end RootSet

section Adjoin

variable {F : Type u} [Field F] {E : Type v} [Field E] [Algebra F E] {a : F} {δ : E}

/-- Over a field, a square root `δ` of `a` generates the whole splitting field of `X ^ 2 - C a`,
because the only other root is `-δ`. -/
theorem _root_.IntermediateField.adjoin_rootSet_X_pow_two_sub_C
    (hδ : δ ^ 2 = algebraMap F E a) :
    IntermediateField.adjoin F (((X : F[X]) ^ 2 - C a).rootSet E) = F⟮δ⟯ := by
  have hmem : δ ∈ ((X : F[X]) ^ 2 - C a).rootSet E :=
    (Polynomial.mem_rootSet_X_pow_sub_C two_ne_zero).mpr hδ
  refine le_antisymm (IntermediateField.adjoin_le_iff.mpr fun x hx ↦ ?_)
    (IntermediateField.adjoin_simple_le_iff.mpr (IntermediateField.subset_adjoin F _ hmem))
  have hx' : x ^ 2 = algebraMap F E a := (Polynomial.mem_rootSet_X_pow_sub_C two_ne_zero).mp hx
  rcases sq_eq_sq_iff_eq_or_eq_neg.mp (hx'.trans hδ.symm) with h | h
  · exact h ▸ IntermediateField.mem_adjoin_simple_self F δ
  · exact h ▸ neg_mem (IntermediateField.mem_adjoin_simple_self F δ)

end Adjoin

section RadicalExtension

variable {F : Type u} [Field F] {E : Type v} [Field E] [Algebra F E]

/-- **Finiteness of a radical extension**: if `y` generates `E` over `F` and satisfies
`y ^ n = a` for nonzero `n`, then `E / F` is finite. -/
theorem Algebra.finiteDimensional_of_pow_eq {y : E} {n : ℕ} {a : F}
    (hgen : F⟮y⟯ = ⊤) (hy : y ^ n = algebraMap F E a) (hn : n ≠ 0) :
    FiniteDimensional F E := by
  have hyint : IsIntegral F y := ⟨X ^ n - C a, monic_X_pow_sub_C a hn, by
    rw [eval₂_sub, eval₂_pow, eval₂_X, eval₂_C, hy, sub_self]⟩
  have hfin : FiniteDimensional F F⟮y⟯ := IntermediateField.adjoin.finiteDimensional hyint
  let e := ((IntermediateField.equivOfEq hgen).trans IntermediateField.topEquiv).toLinearEquiv
  exact e.finiteDimensional

/-- **Separability of a radical extension**: if `y` generates `E` over `F`, satisfies
`y ^ n = a`, and both `n` and `a` are nonzero, then `E / F` is separable. -/
theorem Algebra.isSeparable_of_pow_eq {y : E} {n : ℕ} {a : F}
    (hgen : F⟮y⟯ = ⊤) (hy : y ^ n = algebraMap F E a) (hn : (n : F) ≠ 0) (ha : a ≠ 0) :
    Algebra.IsSeparable F E := by
  have hsep : IsSeparable F y :=
    (separable_X_pow_sub_C _ hn ha).of_dvd (minpoly.dvd _ _ (by simp [hy]))
  have hsep' : Algebra.IsSeparable F F⟮y⟯ :=
    (IntermediateField.isSeparable_adjoin_simple_iff_isSeparable _ _).mpr hsep
  let e := (IntermediateField.equivOfEq hgen).trans IntermediateField.topEquiv
  exact AlgEquiv.Algebra.isSeparable e

end RadicalExtension

section Splits

variable {F : Type u} [CommRing F] {E : Type v} [CommRing E] [Algebra F E] {a : F} {δ : E}

/-- A square root of `a` in `E` splits `X ^ 2 - C a` there: the two linear factors are `X - C δ`
and `X + C δ`. Unlike `Polynomial.X_pow_sub_C_splits_of_isPrimitiveRoot` this needs no primitive
root of unity, so it also covers characteristic `2`, where the two factors coincide. -/
theorem _root_.Polynomial.splits_map_X_pow_two_sub_C (hδ : δ ^ 2 = algebraMap F E a) :
    (((X : F[X]) ^ 2 - C a).map (algebraMap F E)).Splits := by
  have hmap : ((X : F[X]) ^ 2 - C a).map (algebraMap F E) = (X - C δ) * (X - C (-δ)) := by
    rw [Polynomial.map_sub, Polynomial.map_pow, map_X, map_C, ← hδ, map_pow, map_neg]
    ring
  rw [hmap]
  exact (Splits.X_sub_C δ).mul (Splits.X_sub_C (-δ))

end Splits

section Valuation

variable {F : Type u} [Field F]

/-- **A valuative irreducibility criterion for `X ^ n - C a`**: if `n ≠ 0` is coprime to the
order of `a` at a discrete valuation `v`, then `X ^ n - C a` is irreducible. No root of unity and
no hypothesis on the characteristic is needed. For `a = 0` the order is the junk value `0`, so the
hypothesis forces `n = 1`, where the statement holds trivially. See Andrew Yang's
`X_pow_sub_C_irreducible_of_prime` in `Mathlib/FieldTheory/KummerPolynomial.lean`. -/
theorem _root_.Valuation.X_pow_sub_C_irreducible_of_gcd_ord_eq_one (v : Valuation F ℤᵐ⁰)
    {n : ℕ} (hn : n ≠ 0) {a : F} (h : Int.gcd n (v.ord a) = 1) :
    Irreducible (X ^ n - C a) := by
  have hpos : 0 < n := Nat.pos_of_ne_zero hn
  have hne : X ^ n - C a ≠ 0 := X_pow_sub_C_ne_zero hpos a
  have hnu : ¬IsUnit (X ^ n - C a) := by
    rw [Polynomial.isUnit_iff_degree_eq_zero, degree_X_pow_sub_C hpos, Nat.cast_eq_zero]
    exact hn
  obtain ⟨g, hg, hg'⟩ := WfDvdMonoid.exists_irreducible_factor hnu hne
  suffices natDegree g = n from (associated_of_dvd_of_natDegree_le hg' hne
    (this.trans natDegree_X_pow_sub_C.symm).ge).irreducible hg
  -- The norm `N` of a root of `g` satisfies `N ^ n = a ^ deg g`.
  have key : (Algebra.norm F (AdjoinRoot.root g)) ^ n = a ^ g.natDegree := by
    have := eval₂_eq_zero_of_dvd_of_eval₂_eq_zero _ _ hg' (AdjoinRoot.eval₂_root g)
    rw [eval₂_sub, eval₂_pow, eval₂_C, eval₂_X, sub_eq_zero] at this
    rw [← map_pow, this, ← AdjoinRoot.algebraMap_eq, Algebra.norm_algebraMap,
      (AdjoinRoot.powerBasis hg.ne_zero).finrank, AdjoinRoot.powerBasis_dim hg.ne_zero]
  -- Taking orders, `n` divides `deg g · ord_v a`, hence `deg g`.
  have hord : (n : ℤ) * v.ord (Algebra.norm F (AdjoinRoot.root g)) =
      g.natDegree * v.ord a := by
    rw [← Valuation.ord_pow, key, Valuation.ord_pow]
  have hdvd : (n : ℤ) ∣ g.natDegree :=
    Int.dvd_of_dvd_mul_left_of_gcd_one ⟨_, hord.symm⟩ h
  have hle : g.natDegree ≤ n :=
    (natDegree_le_of_dvd hg' hne).trans_eq natDegree_X_pow_sub_C
  have hdeg : 0 < g.natDegree := natDegree_pos_iff_degree_pos.mpr (degree_pos_of_irreducible hg)
  exact hle.antisymm (Nat.le_of_dvd hdeg (Int.natCast_dvd_natCast.mp hdvd))

/-- **The degree of a radical extension**: if `y` generates `E` over `F` with `y ^ n = a`,
and `n` is coprime to the order of `a` at a discrete valuation, then `[E : F] = n`.
This is the degree criterion used in Stichtenoth, Proposition 3.7.3. -/
theorem _root_.Valuation.finrank_eq_of_pow_eq_of_gcd_ord_eq_one
    {E : Type v} [Field E] [Algebra F E] (w : Valuation F ℤᵐ⁰)
    {y : E} {n : ℕ} {a : F} (htop : F⟮y⟯ = ⊤)
    (hy : y ^ n = algebraMap F E a) (hn : n ≠ 0)
    (h : Int.gcd n (w.ord a) = 1) : Module.finrank F E = n := by
  have hirr : Irreducible (X ^ n - C a) :=
    w.X_pow_sub_C_irreducible_of_gcd_ord_eq_one hn h
  have hroot : aeval y (X ^ n - C a) = 0 := by rw [map_sub, aeval_X_pow, aeval_C, hy, sub_self]
  have hint : IsIntegral F y := ⟨X ^ n - C a, monic_X_pow_sub_C a hn, by rwa [← aeval_def]⟩
  have hmin : X ^ n - C a = minpoly F y :=
    minpoly.eq_of_irreducible_of_monic hirr hroot (monic_X_pow_sub_C a hn)
  rw [← IntermediateField.finrank_top', ← htop, IntermediateField.adjoin.finrank hint, ← hmin,
    natDegree_X_pow_sub_C]

end Valuation

end TauCeti
