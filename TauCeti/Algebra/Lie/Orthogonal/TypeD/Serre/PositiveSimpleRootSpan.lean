/-
Copyright (c) 2026 The Tau Ceti contributors. All rights reserved.
Released under Apache 2.0 license as described in the file LICENSE.
Authors: The Tau Ceti contributors
-/
module

public import TauCeti.Algebra.Lie.Presentation.Serre

/-!
# The positive simple-root span in the type-D Serre presentation

This file packages the Lie subalgebra generated by the positive simple-root vectors of the
type-`D` Serre presentation. Its universal property turns generatorwise containment into
containment of the full Lie span, and in any representation it promotes generatorwise
annihilation of a vector to annihilation by that span.

## Main results

* `TauCeti.TypeDStd.positiveSimpleRootLieSpan_le_iff` characterizes containment of the span.
* `TauCeti.TypeDStd.positiveSimpleRootLieSpan_le_lieAnnihilator` applies that characterization to
  an arbitrary Lie module.
-/

public section

namespace TauCeti.TypeDStd

universe u

/-- The Lie subalgebra generated by the positive simple-root vectors in the type-`D` Serre
presentation. -/
noncomputable def positiveSimpleRootLieSpan (n : ℕ) :
    LieSubalgebra ℚ (Matrix.ToLieAlgebra ℚ (CartanMatrix.D n)) :=
  LieSubalgebra.lieSpan ℚ _
    (Set.range (fun i : Fin n => TauCeti.serreE ℚ (CartanMatrix.D n) i))

/-- Every positive simple-root vector belongs to their generated Lie subalgebra. -/
@[simp]
theorem serreE_mem_positiveSimpleRootLieSpan (n : ℕ) (i : Fin n) :
    TauCeti.serreE ℚ (CartanMatrix.D n) i ∈ positiveSimpleRootLieSpan n := by
  exact LieSubalgebra.subset_lieSpan (Set.mem_range_self i)

/-- The positive simple-root span is contained in a Lie subalgebra exactly when every positive
simple generator belongs to it. -/
theorem positiveSimpleRootLieSpan_le_iff (n : ℕ)
    (S : LieSubalgebra ℚ (Matrix.ToLieAlgebra ℚ (CartanMatrix.D n))) :
    positiveSimpleRootLieSpan n ≤ S ↔
      ∀ i : Fin n, TauCeti.serreE ℚ (CartanMatrix.D n) i ∈ S := by
  rw [positiveSimpleRootLieSpan, LieSubalgebra.lieSpan_le, Set.range_subset_iff]
  rfl

/-- In any representation of the type-`D` Serre presentation, a vector annihilated by every
positive simple-root vector is annihilated by their Lie span. -/
theorem positiveSimpleRootLieSpan_le_lieAnnihilator
    (n : ℕ) {M : Type u} [AddCommGroup M] [Module ℚ M]
    [LieRingModule (Matrix.ToLieAlgebra ℚ (CartanMatrix.D n)) M]
    [LieModule ℚ (Matrix.ToLieAlgebra ℚ (CartanMatrix.D n)) M]
    (v : M) (hv : ∀ i : Fin n, ⁅TauCeti.serreE ℚ (CartanMatrix.D n) i, v⁆ = 0) :
    positiveSimpleRootLieSpan n ≤ TauCeti.lieAnnihilator ℚ _ v := by
  rw [positiveSimpleRootLieSpan_le_iff]
  intro i
  exact (TauCeti.mem_lieAnnihilator ℚ _).mpr (hv i)

end TauCeti.TypeDStd
