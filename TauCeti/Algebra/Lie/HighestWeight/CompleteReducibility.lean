/-
Copyright (c) 2026 The Tau Ceti contributors. All rights reserved.
Released under Apache 2.0 license as described in the file LICENSE.
Authors: The Tau Ceti contributors
-/
module

public import Mathlib.FieldTheory.IsAlgClosed.Basic
public import TauCeti.Algebra.Lie.CompleteReducibility
public import TauCeti.Algebra.Lie.UniversalEnveloping.Casimir
-- Non-public: these appear only inside proofs, never in the type of an exported declaration.
import Mathlib.Algebra.Lie.CartanExists
import Mathlib.LinearAlgebra.RootSystem.BaseExists
import TauCeti.Algebra.Lie.HighestWeight.Existence
import TauCeti.Algebra.Lie.HighestWeight.Separation
import TauCeti.Algebra.Lie.HighestWeight.Trivial
import TauCeti.Algebra.Lie.Killing.Perfect
import TauCeti.Algebra.Lie.Submodule.Decomposition

/-!
# Weyl's complete reducibility theorem

Let `L` be a finite-dimensional Lie algebra with nondegenerate Killing form over an algebraically
closed field of characteristic zero. **Every Lie submodule of a finite-dimensional `L`-module is a
direct summand** (`TauCeti.exists_isCompl_of_isKilling`), so the lattice of Lie submodules is
complemented and a finite-dimensional module is a direct sum of irreducibles.

All the formal content of the proof is already in
`TauCeti/Algebra/Lie/CompleteReducibility.lean`, which derives complete reducibility from the
single representation-theoretic input `TauCeti.HasInvariantOutsideIrreducible`: whenever `L`
carries a finite-dimensional module `M` into a proper *irreducible* submodule `N` and acts
nontrivially somewhere on `M`, the module `M` has a nonzero invariant vector outside `N`. This
file supplies that input from the Casimir element of `U(L)`, which is what makes the argument work
for a general semisimple `L` rather than only for `sl₂`
(`TauCeti/Algebra/Lie/Sl2/CompleteReducibility.lean`, the rank-one instance of the same
reduction).

## The Casimir step

Two facts about the Casimir element `Ω ∈ U(L)` do the work.

* `Ω` acts on `M` by a sum of double brackets `∑ᵢ ⁅xᵢ, ⁅yᵢ, -⁆⁆`, so `⁅L, M⁆ ⊆ N` already forces
  its image to lie in `N`.
* `Ω` is **injective** on a nontrivial finite-dimensional irreducible module
  (`TauCeti.representation_casimirElement_injective_of_isIrreducible_of_not_isTrivial`): such a
  module is generated by a highest weight vector of a dominant integral weight `λ`
  (`TauCeti.exists_isHighestWeightVector_and_lieSpan_eq_top`), and `λ ≠ 0` because a highest weight
  vector of weight zero generates a trivial module
  (`TauCeti.isTrivial_of_isHighestWeightVector_weight_zero_of_isIrreducible`); the Casimir then
  acts by the nonzero scalar `⟨λ + ρ, λ + ρ⟩ - ⟨ρ, ρ⟩`
  (`TauCeti.casimir_apply_ne_zero_of_isHighestWeightVector_of_lieSpan_eq_top`).

Being injective on `N` and having its image inside the proper submodule `N`, the Casimir operator
of `M` is not surjective, hence — `M` being finite-dimensional — not injective; a nonzero kernel
vector lies outside `N`, and its brackets lie in `N ∩ ker Ω = 0`, so it is invariant.

The remaining case is the one where `L` acts trivially on `N`. Then the action of `L` on `M`
composes to zero, and a Lie algebra with nondegenerate Killing form is perfect
(`TauCeti.derivedSeries_one_eq_top_of_isKilling`), so the action is already zero, contradicting the
hypothesis that `L` acts nontrivially somewhere on `M`.

## Main results

* `TauCeti.representation_casimirElement_injective_of_isIrreducible_of_not_isTrivial`: the Casimir
  element acts injectively on a finite-dimensional irreducible module with a nontrivial action.
* `TauCeti.hasInvariantOutsideIrreducible_of_isKilling`: the Casimir element supplies the input of
  the formal complete-reducibility argument.
* `TauCeti.exists_invariant_notMem_of_isKilling`: a module carried into a proper Lie submodule has
  an invariant vector outside it.
* `TauCeti.exists_isCompl_of_isKilling`: **Weyl's complete reducibility theorem.** Every Lie
  submodule of a finite-dimensional module is a direct summand.
* `TauCeti.complementedLattice_lieSubmodule_of_isKilling`: the lattice restatement.
* `TauCeti.exists_isInternal_isIrreducible_of_isKilling`: the decomposition restatement, a
  finite-dimensional module as an internal direct sum of irreducibles.

## References

This is the summit of Layer 5, "complete reducibility (Weyl's theorem)", of
`TauCetiRoadmap/RepresentationTheory/LieHighestWeight/README.md`.

* J. E. Humphreys, *Introduction to Lie Algebras and Representation Theory*, GTM 9, §6.3, for the
  Casimir proof of Weyl's theorem; the reduction to the case of a codimension-one submodule is the
  formal part carried out in `TauCeti/Algebra/Lie/CompleteReducibility.lean`.
-/

public section

namespace TauCeti

open LieAlgebra LieModule Module

universe u v w

variable {K : Type u} {L : Type v} [Field K] [CharZero K] [IsAlgClosed K]
  [LieRing L] [LieAlgebra K L] [IsKilling K L] [FiniteDimensional K L]

/-! ### The Casimir element is injective on a nontrivial irreducible -/

section Injective

variable (M : Type w) [AddCommGroup M] [Module K M] [LieRingModule L M] [LieModule K L M]
  [FiniteDimensional K M]

/-- **The Casimir element is injective on a nontrivial finite-dimensional irreducible module.**
The module is generated by a highest weight vector, whose weight is dominant integral and, the
action being nontrivial, nonzero; the Casimir element then acts by a nonzero scalar. -/
theorem representation_casimirElement_injective_of_isIrreducible_of_not_isTrivial
    [LieModule.IsIrreducible K L M] (hact : ¬ LieModule.IsTrivial L M) :
    Function.Injective (UniversalEnvelopingAlgebra.representation K L M (casimirElement K L)) := by
  obtain ⟨x₀, hx₀⟩ := LieAlgebra.exists_isCartanSubalgebra_engel K L
  have _i : (LieSubalgebra.engel K x₀).IsCartanSubalgebra := hx₀
  obtain ⟨b⟩ := (IsKilling.rootSystem (LieSubalgebra.engel K x₀)).nonempty_base
  obtain ⟨lam, v, hv, hgen⟩ := exists_isHighestWeightVector_and_lieSpan_eq_top (M := M) b
  have hlam : lam ≠ 0 := by
    rintro rfl
    exact hact (isTrivial_of_isHighestWeightVector_weight_zero_of_isIrreducible hv)
  refine (injective_iff_map_eq_zero _).2 fun m hm ↦ ?_
  by_contra hne
  exact casimir_apply_ne_zero_of_isHighestWeightVector_of_lieSpan_eq_top hv hgen
    hv.isDominantIntegral hlam hne hm

end Injective

/-! ### Complete reducibility -/

variable (K L) in
/-- **The Casimir element supplies an invariant vector outside an irreducible submodule.** If `L`
carries `M` into a proper irreducible `N` and acts nontrivially somewhere on `M`, then `L` acts
nontrivially on `N`, so the Casimir element is injective on `N`. Its image lies in `N`, so it is
not surjective on `M`, hence — `M` being finite-dimensional — not injective, and any nonzero
kernel vector is invariant and outside `N`.

This is the single input of `TauCeti/Algebra/Lie/CompleteReducibility.lean`; everything else in
the proof of complete reducibility is formal. -/
theorem hasInvariantOutsideIrreducible_of_isKilling :
    HasInvariantOutsideIrreducible.{w} K L := by
  unfold HasInvariantOutsideIrreducible
  intro M _ _ _ _ _ N hN htriv hact hirr
  have hactN : ¬ LieModule.IsTrivial L (↥N) := by
    intro hNtriv
    obtain ⟨x, m, hm⟩ := hact
    refine hm ((isTrivial_of_derivedSeries_one_eq_top_of_lie_lie_eq_zero
      (derivedSeries_one_eq_top_of_isKilling K L) fun y z w ↦ ?_).trivial x m)
    have hval := congrArg (Subtype.val : N → M) (hNtriv.trivial y ⟨⁅z, w⁆, htriv z w⟩)
    rw [LieSubmodule.coe_bracket] at hval
    simpa using hval
  have hinjN := representation_casimirElement_injective_of_isIrreducible_of_not_isTrivial
    (K := K) (↥N) hactN
  have hinj : ∀ w : M, w ∈ N →
      UniversalEnvelopingAlgebra.representation K L M (casimirElement K L) w = 0 → w = 0 := by
    intro w hw hw0
    have hcoe := map_representation_casimirElement N.incl (⟨w, hw⟩ : N)
    simp only [LieSubmodule.incl_apply] at hcoe
    have hz : (⟨w, hw⟩ : N) = 0 := hinjN (Subtype.ext (by rw [hcoe]; simpa using hw0))
    exact congrArg Subtype.val hz
  have hrange : ∀ m : M,
      UniversalEnvelopingAlgebra.representation K L M (casimirElement K L) m ∈ N := by
    intro m
    rw [casimirElement_eq_sum (Module.finBasis K L)]
    simp only [map_sum, LinearMap.sum_apply, map_mul, Module.End.mul_apply,
      UniversalEnvelopingAlgebra.representation_ι, LieModule.toEnd_apply_apply]
    exact sum_mem fun i _ ↦ N.lie_mem (htriv _ m)
  have hnotinj : ¬ Function.Injective
      (UniversalEnvelopingAlgebra.representation K L M (casimirElement K L)) := by
    intro hi
    refine hN ((LieSubmodule.toSubmodule_inj _ _).1 ?_)
    rw [LieSubmodule.top_toSubmodule, eq_top_iff]
    intro m _
    obtain ⟨w, rfl⟩ := LinearMap.injective_iff_surjective.1 hi m
    exact hrange w
  obtain ⟨v, hvmem, hv0⟩ := (Submodule.ne_bot_iff _).1
    (fun hc ↦ hnotinj (LinearMap.ker_eq_bot.1 hc))
  have hv : UniversalEnvelopingAlgebra.representation K L M (casimirElement K L) v = 0 := hvmem
  refine ⟨v, fun hc ↦ hv0 (hinj v hc hv), fun x ↦ ?_⟩
  refine hinj _ (htriv x v) ?_
  rw [representation_casimirElement_lie, hv, lie_zero]

variable {M : Type w} [AddCommGroup M] [Module K M] [LieRingModule L M] [LieModule K L M]

/-- **An invariant vector outside a proper submodule.** If `L` carries a finite-dimensional module
`M` into a proper Lie submodule `N`, then `M` has a nonzero invariant vector outside `N`. -/
theorem exists_invariant_notMem_of_isKilling [FiniteDimensional K M]
    (N : LieSubmodule K L M) (hN : N ≠ ⊤) (htriv : ∀ (x : L) (m : M), ⁅x, m⁆ ∈ N) :
    ∃ v : M, v ∉ N ∧ ∀ x : L, ⁅x, v⁆ = 0 :=
  HasInvariantOutsideIrreducible.exists_invariant_notMem
    (hasInvariantOutsideIrreducible_of_isKilling K L) N hN htriv

/-- **Weyl's complete reducibility theorem.** Over an algebraically closed field of characteristic
zero, every Lie submodule of a finite-dimensional module over a Lie algebra with nondegenerate
Killing form has a complement, so the module is a direct sum of irreducibles.

The `sl₂` case, `TauCeti.exists_isCompl_of_toLieSubalgebra_eq_top`, is the rank-one instance of
the same argument, run with the concrete Casimir operator of an `sl₂` triple. -/
theorem exists_isCompl_of_isKilling [FiniteDimensional K M] (N : LieSubmodule K L M) :
    ∃ N' : LieSubmodule K L M, IsCompl N N' :=
  HasInvariantOutsideIrreducible.exists_isCompl
    (hasInvariantOutsideIrreducible_of_isKilling K L) N

variable (K L M) in
/-- **The lattice form of Weyl's theorem**: the Lie submodules of a finite-dimensional module over
a Lie algebra with nondegenerate Killing form form a complemented lattice. -/
theorem complementedLattice_lieSubmodule_of_isKilling [FiniteDimensional K M] :
    ComplementedLattice (LieSubmodule K L M) :=
  ⟨exists_isCompl_of_isKilling⟩

variable (K L M) in
/-- **Weyl's theorem as a decomposition.** A finite-dimensional module over a Lie algebra with
nondegenerate Killing form, over an algebraically closed field of characteristic zero, is the
internal direct sum of a finite family of irreducible Lie submodules. -/
theorem exists_isInternal_isIrreducible_of_isKilling [FiniteDimensional K M] :
    ∃ (k : ℕ) (N : Fin k → LieSubmodule K L M),
      DirectSum.IsInternal (fun i ↦ (N i).toSubmodule) ∧
        ∀ i, LieModule.IsIrreducible K L (N i) :=
  have _i := complementedLattice_lieSubmodule_of_isKilling K L M
  exists_isInternal_isIrreducible K L M

end TauCeti
