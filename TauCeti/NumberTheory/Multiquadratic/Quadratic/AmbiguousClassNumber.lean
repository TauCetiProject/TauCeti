/-
Copyright (c) 2026 The Tau Ceti contributors. All rights reserved.
Released under Apache 2.0 license as described in the file LICENSE.
Authors: The Tau Ceti contributors
-/
module

import TauCeti.NumberTheory.Multiquadratic.Quadratic.GenusCharacter.OrdinaryTwoRank
public import TauCeti.NumberTheory.Multiquadratic.Quadratic.TwoRank
public import TauCeti.NumberTheory.NumberField.Quadratic.Conjugation.Ambiguous.Narrow
import Mathlib.NumberTheory.NumberField.ClassNumber
import TauCeti.NumberTheory.NumberField.Quadratic.InfinitePlace

/-!
# The ambiguous class number formula for a quadratic field

Let `K = ℚ(√d)` with `d` squarefree, and let `t` be the number of rational primes that ramify in
`K`. An ideal class is *ambiguous* when it is fixed by the quadratic conjugation `σ`;
since `σ` acts on `Cl(K)` by inversion, the ambiguous classes are exactly the `2`-torsion classes
(`NumberField.mulEquiv_ringOfIntegersQuadraticConj_apply_eq_self_iff`), and for an imaginary field
each of them is the class of an *ambiguous ideal* `I = σI`
(`NumberField.sq_eq_one_iff_exists_map_ringOfIntegersQuadraticConj_eq_self`). In the narrow class
group `Cl⁺(K)` of a field of either signature, the `2`-torsion classes are likewise exactly the
narrow classes of ambiguous ideals
(`NumberField.NarrowClassGroup.sq_eq_one_iff_exists_map_ringOfIntegersQuadraticConj_eq_self`). The
**ambiguous class number formula** counts them:

`#{C ∈ Cl⁺(K) | C² = 1} = 2 ^ (t - 1)`, and, for `d < 0`,
`#{C ∈ Cl(K) | σC = C} = #{C ∈ Cl(K) | C² = 1} = 2 ^ (t - 1)`.

These are the cardinality forms of the `2`-rank formulas `rank₂ Cl⁺(K) = t - 1`
(`narrowTwoRank_eq_ncard_ramifiedPrimes_sub_one`) and, for imaginary `K`, `rank₂ Cl(K) = t - 1`
(`twoRank_eq_ncard_ramifiedPrimes_sub_one`): the `2`-torsion subgroup and the maximal
elementary-`2` quotient of a finite abelian group have the same size
(`TauCeti.card_elementaryTwoQuotient_eq_card_twoTorsion`).

For a real quadratic field the ordinary class group has two different counts. The ambiguous
classes, those fixed by `σ`, are still the `2`-torsion classes, and their number is read off from
the ordinary `2`-rank (`twoRank_eq_ncard_ramifiedPrimes_sub_one_iff`):

`#{C ∈ Cl(K) | σC = C} = 2 ^ (t - 1)` if every prime discriminant dividing `disc K` is positive,
and `2 ^ (t - 2)` otherwise.

The *strongly* ambiguous classes, those of ambiguous ideals, are the image of the `2`-torsion of
`Cl⁺(K)` under `Cl⁺(K) → Cl(K)`, whose kernel is generated by the narrow class of `(√d)`; so their
number depends on the norms of the units instead:

`#{[I] ∈ Cl(K) | σI = I} = 2 ^ (t - 1)` if some unit has norm `-1`, and `2 ^ (t - 2)` otherwise.

These are the two cases of Chevalley's formulas `2 ^ (t - 1) / [E_ℚ : E_ℚ ∩ N K^×]` and
`2 ^ (t - 1) / [E_ℚ : N E_K]` with `E_ℚ = {±1}`, the sign condition on the prime discriminants
being the local condition for `-1` to be a norm from `K`. The two counts differ for `ℚ(√34)`,
where `-1` is a norm but not the norm of a unit.

The formulas are classical; see D. A. Cox, *Primes of the Form x² + ny²*, §3.B and §6.A, and
F. Lemmermeyer, *Reciprocity Laws: From Euler to Eisenstein*, §2.2.

## Main results

* `TauCeti.Multiquadratic.natCard_classGroup_sq_eq_one_eq_two_pow`: an imaginary quadratic field
  has exactly `2 ^ (t - 1)` ideal classes of order dividing `2`.
* `TauCeti.Multiquadratic.natCard_mulEquiv_ringOfIntegersQuadraticConj_eq_self_eq_two_pow`: the
  same count for the classes fixed by quadratic conjugation, the ambiguous classes.
* `TauCeti.Multiquadratic.natCard_exists_map_ringOfIntegersQuadraticConj_eq_self_eq_two_pow`: the
  same count for the classes of ambiguous ideals.
* `TauCeti.Multiquadratic.natCard_narrowClassGroup_sq_eq_one_eq_two_pow`: a quadratic field of
  either signature has exactly `2 ^ (t - 1)` narrow ideal classes of order dividing `2`.
* `natCard_narrowClassGroup_exists_map_ringOfIntegersQuadraticConj_eq_self_eq_two_pow` (same
  namespace) counts the same narrow classes as narrow classes of ambiguous ideals.
* `natCard_mulEquiv_ringOfIntegersQuadraticConj_eq_self_eq_two_pow_of_forall_pos` and
  `natCard_mulEquiv_ringOfIntegersQuadraticConj_eq_self_eq_two_pow_of_neg` (same namespace): the
  ambiguous class number of a real quadratic field.
* `NumberField.NarrowClassGroup.natCard_isStronglyAmbiguousClass_mul_natCard_ker`: for either
  signature, the number of strongly ambiguous classes times the order of the kernel of
  `Cl⁺(K) → Cl(K)` is the narrow ambiguous class number.
* `natCard_isStronglyAmbiguousClass_eq_two_pow_of_norm_eq_neg_one` and
  `natCard_isStronglyAmbiguousClass_eq_two_pow_of_forall_norm_ne_neg_one`
  (same namespace): the strongly ambiguous class number of a real quadratic field.
-/

public section

open Polynomial NumberField
open scoped NumberField nonZeroDivisors

namespace TauCeti.Multiquadratic

variable {K : Type*} [Field K] [NumberField K] {θ : 𝓞 K} {d : ℤ}

/-- **The ambiguous class number formula for an imaginary quadratic field.** For `K = ℚ(√d)` with
`d < 0` squarefree, the number of ideal classes `C` with `C ^ 2 = 1` is `2 ^ (t - 1)`, where `t` is
the number of rational primes ramifying in `K`. -/
theorem natCard_classGroup_sq_eq_one_eq_two_pow
    (hmin : minpoly ℤ θ = X ^ 2 - C d) (hgen : Algebra.adjoin ℚ {(θ : K)} = ⊤)
    (hsf : Squarefree d) (hd : d < 0) :
    Nat.card {C : ClassGroup (𝓞 K) // C ^ 2 = 1} = 2 ^ ((ramifiedPrimes K).ncard - 1) := by
  rw [← TauCeti.ClassGroup.card_elementaryTwoQuotient_eq_card_twoTorsion,
    TauCeti.ClassGroup.card_elementaryTwoQuotient_eq_two_pow_twoRank,
    twoRank_eq_ncard_ramifiedPrimes_sub_one hmin hgen hsf hd]

/-- **The ambiguous class number formula, counted by conjugation-fixed classes.** For `K = ℚ(√d)`
with `d < 0` squarefree, exactly `2 ^ (t - 1)` ideal classes are fixed by the quadratic
conjugation, where `t` is the number of rational primes ramifying in `K`. -/
theorem natCard_mulEquiv_ringOfIntegersQuadraticConj_eq_self_eq_two_pow
    (hmin : minpoly ℤ θ = X ^ 2 - C d) (hgen : Algebra.adjoin ℚ {(θ : K)} = ⊤)
    (hsf : Squarefree d) (hd : d < 0) :
    Nat.card {C : ClassGroup (𝓞 K) //
      ClassGroup.mulEquiv (ringOfIntegersQuadraticConj hmin hgen) C = C} =
      2 ^ ((ramifiedPrimes K).ncard - 1) := by
  rw [← natCard_classGroup_sq_eq_one_eq_two_pow hmin hgen hsf hd]
  exact Nat.card_congr (Equiv.subtypeEquivRight fun C =>
    mulEquiv_ringOfIntegersQuadraticConj_apply_eq_self_iff hmin hgen C)

/-- **The ambiguous class number formula, counted by ambiguous ideals.** For `K = ℚ(√d)` with
`d < 0` squarefree, exactly `2 ^ (t - 1)` ideal classes are represented by an ideal fixed by the
quadratic conjugation, where `t` is the number of rational primes ramifying in `K`. -/
theorem natCard_exists_map_ringOfIntegersQuadraticConj_eq_self_eq_two_pow
    (hmin : minpoly ℤ θ = X ^ 2 - C d) (hgen : Algebra.adjoin ℚ {(θ : K)} = ⊤)
    (hsf : Squarefree d) (hd : d < 0) :
    Nat.card {C : ClassGroup (𝓞 K) // ∃ I : (Ideal (𝓞 K))⁰,
      Ideal.map (ringOfIntegersQuadraticConj hmin hgen) (I : Ideal (𝓞 K)) = (I : Ideal (𝓞 K)) ∧
        ClassGroup.mk0 I = C} = 2 ^ ((ramifiedPrimes K).ncard - 1) := by
  have : IsTotallyComplex K := isTotallyComplex_of_minpoly_eq_X_sq_sub_C_of_neg hmin hd
  rw [← natCard_classGroup_sq_eq_one_eq_two_pow hmin hgen hsf hd]
  exact Nat.card_congr (Equiv.subtypeEquivRight fun C =>
    (sq_eq_one_iff_exists_map_ringOfIntegersQuadraticConj_eq_self hmin hgen C).symm)

/-! ### The narrow class group, for either signature -/

/-- **The narrow ambiguous class number formula.** For `K = ℚ(√d)` with `d` squarefree, of either
signature, the number of narrow ideal classes `C` with `C ^ 2 = 1` is `2 ^ (t - 1)`, where `t` is
the number of rational primes ramifying in `K`. -/
theorem natCard_narrowClassGroup_sq_eq_one_eq_two_pow
    (hmin : minpoly ℤ θ = X ^ 2 - C d) (hgen : Algebra.adjoin ℚ {(θ : K)} = ⊤)
    (hsf : Squarefree d) :
    Nat.card {C : NarrowClassGroup K // C ^ 2 = 1} = 2 ^ ((ramifiedPrimes K).ncard - 1) := by
  rw [← TauCeti.card_elementaryTwoQuotient_eq_card_twoTorsion,
    NarrowClassGroup.card_elementaryTwoQuotient_eq_two_pow_twoRank,
    narrowTwoRank_eq_ncard_ramifiedPrimes_sub_one hmin hgen hsf]

/-- **The narrow ambiguous class number formula, counted by ambiguous ideals.** For `K = ℚ(√d)`
with `d` squarefree, of either signature, exactly `2 ^ (t - 1)` narrow ideal classes are
represented by an ideal fixed by the quadratic conjugation, where `t` is the number of rational
primes ramifying in `K`. -/
theorem natCard_narrowClassGroup_exists_map_ringOfIntegersQuadraticConj_eq_self_eq_two_pow
    (hmin : minpoly ℤ θ = X ^ 2 - C d) (hgen : Algebra.adjoin ℚ {(θ : K)} = ⊤)
    (hsf : Squarefree d) :
    Nat.card {C : NarrowClassGroup K // ∃ I : (Ideal (𝓞 K))⁰,
      Ideal.map (ringOfIntegersQuadraticConj hmin hgen) (I : Ideal (𝓞 K)) = (I : Ideal (𝓞 K)) ∧
        NarrowClassGroup.mk0 I = C} = 2 ^ ((ramifiedPrimes K).ncard - 1) := by
  rw [← natCard_narrowClassGroup_sq_eq_one_eq_two_pow hmin hgen hsf]
  exact Nat.card_congr (Equiv.subtypeEquivRight fun C =>
    (NarrowClassGroup.sq_eq_one_iff_exists_map_ringOfIntegersQuadraticConj_eq_self hmin hgen
      C).symm)

/-! ### The ordinary class group of a real quadratic field -/

/-- **The ambiguous class number formula for a real quadratic field, counted by
conjugation-fixed classes.** For `K = ℚ(√d)` with `d > 0` squarefree whose prime discriminants are
all positive, exactly `2 ^ (t - 1)` ideal classes are fixed by the quadratic conjugation. -/
theorem natCard_mulEquiv_ringOfIntegersQuadraticConj_eq_self_eq_two_pow_of_forall_pos
    {s : Finset ℤ} (hs : ∀ P ∈ s, IsPrimeDiscriminant P)
    (heven : ∀ P ∈ s, ∀ P' ∈ s, IsEvenPrimeDiscriminant P → IsEvenPrimeDiscriminant P' → P = P')
    (hprod : ∏ P ∈ s, P = fundamentalDiscriminant d)
    (hmin : minpoly ℤ θ = X ^ 2 - C d) (hgen : Algebra.adjoin ℚ {(θ : K)} = ⊤)
    (hsf : Squarefree d) (hpos : ∀ P ∈ s, 0 < P) :
    Nat.card {C : ClassGroup (𝓞 K) //
      ClassGroup.mulEquiv (ringOfIntegersQuadraticConj hmin hgen) C = C} =
      2 ^ ((ramifiedPrimes K).ncard - 1) := by
  have hd : 0 < d := by
    have hfd : 0 < fundamentalDiscriminant d := by
      rw [← hprod]
      exact Finset.prod_pos hpos
    obtain ⟨c, hc, hcfd⟩ := exists_sq_mul_eq_fundamentalDiscriminant d
    rcases hc with rfl | rfl <;> norm_num at hcfd <;> omega
  rw [Nat.card_congr (Equiv.subtypeEquivRight fun C =>
      mulEquiv_ringOfIntegersQuadraticConj_apply_eq_self_iff hmin hgen C),
    ← TauCeti.ClassGroup.card_elementaryTwoQuotient_eq_card_twoTorsion,
    TauCeti.ClassGroup.card_elementaryTwoQuotient_eq_two_pow_twoRank,
    (twoRank_eq_ncard_ramifiedPrimes_sub_one_iff hs heven hprod hmin hgen hsf hd).mpr hpos]

/-- **The ambiguous class number formula for a real quadratic field with a negative prime
discriminant, counted by conjugation-fixed classes.** For `K = ℚ(√d)` with `d > 0` squarefree and
some negative prime discriminant `P ∈ s`, exactly `2 ^ (t - 2)` ideal classes are fixed by the
quadratic conjugation. -/
theorem natCard_mulEquiv_ringOfIntegersQuadraticConj_eq_self_eq_two_pow_of_neg {s : Finset ℤ}
    (hs : ∀ P ∈ s, IsPrimeDiscriminant P)
    (heven : ∀ P ∈ s, ∀ P' ∈ s, IsEvenPrimeDiscriminant P → IsEvenPrimeDiscriminant P' → P = P')
    (hprod : ∏ P ∈ s, P = fundamentalDiscriminant d)
    (hmin : minpoly ℤ θ = X ^ 2 - C d) (hgen : Algebra.adjoin ℚ {(θ : K)} = ⊤)
    (hsf : Squarefree d) (hd : 0 < d) {P : ℤ} (hP : P ∈ s) (hneg : P < 0) :
    Nat.card {C : ClassGroup (𝓞 K) //
      ClassGroup.mulEquiv (ringOfIntegersQuadraticConj hmin hgen) C = C} =
      2 ^ ((ramifiedPrimes K).ncard - 2) := by
  rw [Nat.card_congr (Equiv.subtypeEquivRight fun C =>
      mulEquiv_ringOfIntegersQuadraticConj_apply_eq_self_iff hmin hgen C),
    ← TauCeti.ClassGroup.card_elementaryTwoQuotient_eq_card_twoTorsion,
    TauCeti.ClassGroup.card_elementaryTwoQuotient_eq_two_pow_twoRank,
    twoRank_eq_ncard_ramifiedPrimes_sub_two_of_neg hs heven hprod hmin hgen hsf hd hP hneg]

/-- **The strongly ambiguous class number of a quadratic field with a unit of norm `-1`.** For
`K = ℚ(√d)` with `d` squarefree, if some unit of `𝓞 K` has norm `-1` (so `K` is real), then exactly
`2 ^ (t - 1)` ideal classes are represented by an ideal fixed by the quadratic conjugation, where
`t` is the number of rational primes ramifying in `K`. -/
theorem natCard_isStronglyAmbiguousClass_eq_two_pow_of_norm_eq_neg_one
    (hmin : minpoly ℤ θ = X ^ 2 - C d) (hgen : Algebra.adjoin ℚ {(θ : K)} = ⊤)
    (hsf : Squarefree d) {u : (𝓞 K)ˣ} (hu : Algebra.norm ℚ ((u : 𝓞 K) : K) = -1) :
    Nat.card (stronglyAmbiguousClassSubgroup hmin hgen) =
      2 ^ ((ramifiedPrimes K).ncard - 1) := by
  have key := NarrowClassGroup.natCard_isStronglyAmbiguousClass_mul_natCard_ker hmin hgen
  rw [(MonoidHom.ker_eq_bot_iff _).mpr
      (NarrowClassGroup.toClassGroup_injective_of_norm_eq_neg_one hmin hgen hu),
    Subgroup.card_bot, mul_one, natCard_narrowClassGroup_sq_eq_one_eq_two_pow hmin hgen hsf] at key
  exact key

/-- **The strongly ambiguous class number of a real quadratic field without a unit of norm
`-1`.** For `K = ℚ(√d)` with `d > 0` squarefree, if no unit of `𝓞 K` has norm `-1`, then exactly
`2 ^ (t - 2)` ideal classes are represented by an ideal fixed by the quadratic conjugation, where
`t` is the number of rational primes ramifying in `K`: the narrow class of `(√d)` is then
nontrivial, and it is an ambiguous class that dies in `Cl(K)`. -/
theorem natCard_isStronglyAmbiguousClass_eq_two_pow_of_forall_norm_ne_neg_one
    (hmin : minpoly ℤ θ = X ^ 2 - C d) (hgen : Algebra.adjoin ℚ {(θ : K)} = ⊤)
    (hsf : Squarefree d) (hd : 0 < d)
    (hu : ∀ u : (𝓞 K)ˣ, Algebra.norm ℚ ((u : 𝓞 K) : K) ≠ -1) :
    Nat.card (stronglyAmbiguousClassSubgroup hmin hgen) =
      2 ^ ((ramifiedPrimes K).ncard - 2) := by
  have key := NarrowClassGroup.natCard_isStronglyAmbiguousClass_mul_natCard_ker hmin hgen
  -- Without a unit of norm `-1` the kernel of `Cl⁺(K) → Cl(K)` is nontrivial, of order `2`.
  have hne : (NarrowClassGroup.toClassGroup (K := K)).ker ≠ ⊥ := fun h => by
    obtain ⟨u, hu'⟩ := (NarrowClassGroup.toClassGroup_injective_iff_exists_norm_eq_neg_one hmin
      hgen hd).mp ((MonoidHom.ker_eq_bot_iff _).mp h)
    exact hu u hu'
  have h2 : Nat.card (NarrowClassGroup.toClassGroup (K := K)).ker = 2 :=
    le_antisymm (card_ker_toClassGroup_le_two (finrank_rat_eq_two hmin hgen))
      (Subgroup.one_lt_card_iff_ne_bot _ |>.mpr hne)
  rw [h2, natCard_narrowClassGroup_sq_eq_one_eq_two_pow hmin hgen hsf] at key
  -- `S * 2 = 2 ^ (t - 1)` forces `t ≥ 2`, so the factor `2` can be cancelled.
  have ht : 2 ≤ (ramifiedPrimes K).ncard := by
    by_contra h
    have hsub_zero : (ramifiedPrimes K).ncard - 1 = 0 := by omega
    rw [hsub_zero, pow_zero] at key
    omega
  have hsub_succ : (ramifiedPrimes K).ncard - 1 = (ramifiedPrimes K).ncard - 2 + 1 := by omega
  rw [hsub_succ, pow_succ] at key
  exact Nat.eq_of_mul_eq_mul_right two_pos key

end TauCeti.Multiquadratic
